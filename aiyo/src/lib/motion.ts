"use client";

/**
 * LazyMotion-compatible motion primitives. Import `m` and `AnimatePresence` from here
 * instead of `framer-motion`'s `motion` export.
 */
export { AnimatePresence } from "framer-motion";
import * as m from "framer-motion/m";

export { m };
